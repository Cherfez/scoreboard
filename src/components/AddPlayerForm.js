import React from "react";

export default function AddPlayerForm() {
  return (
    <div className="AddPlayerForm">
      <input className="inputPlayerForm" placeholder="Add New Player"></input>
      <button>Add</button>
    </div>
  );
}
